export default interface User {
    id?: number,
    fullName: String,
    password: String,
    email: String;
    // address:Address,
    // roleId :Roles,
    // companyId: Company,
    // AuditData: AuditData
}