import { styled } from '@mui/system';
import { PALLETE } from '../../config/config';

export const MyTxtField = styled('div')({
    width: '90% !important'
})

export const MyMsdError = styled('div')({
    color: `${PALLETE.RED} !important `,
    fontSize: "10px",
})

