// Loader component
import React from 'react';

const Loader: React.FC = () => {
  return (
    <div className="loader">
      {/* Replace this with your actual loader UI */}
      Loading...
    </div>
  );
};

export default Loader;
